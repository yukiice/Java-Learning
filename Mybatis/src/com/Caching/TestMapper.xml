<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Caching.TestMapper">
<!--    <cache readOnly="true" eviction="FIFO" size="512" flushInterval="60000" />-->
    <select id="selectStudent" resultType="Map">
        select * from testnow
    </select>
    <select id="selectStudentById" resultType="Map" useCache="false" flushCache="true">
        select *
        from testnow where id = #{id}
    </select>
    <insert id="addStudent">
        insert into testnow(name, age) values(#{name}, #{age})
    </insert>
    <delete id="deleteStudent">
        delete from  testnow where  id = #{id};
    </delete>
</mapper>